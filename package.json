{
    "name": "replaced-by-user-input",
    "description": "replaced-by-user-input",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "chrome-aws-lambda": "^10.1.0"
    },
    "devDependencies": {
        "jest": "^26.6.3"
    },
    "scripts": {
        "test": "jest",
        "zip": "rm -rf lambda.zip && zip -r lambda.zip src node_modules package.json package-lock.json"
    }
}
